def my_func(x1, x2, x3):
  if type(x1)== float and type(x2)== float and type(x3)== float :
    x=(x1+x2+x3)*(x2+x3)*x3
    if x==0:
        return ("Not a number – denominator equals zero")
    y=x1+x2+x3
    return(x/y)
  else:
      return ("Error: parameters should be float") 
  
print(my_func(2.0, 1.0, 0.0))
