def convert(hours, minutes):
    if hours < 0 or minutes < 0:
        return ("Input error!")
    else:
        h = int((hours * 60) / 60)
        m=(hours*60)-60
        M = (m+minutes) * 60
        H=h*60*60
        return (M+H)


print(convert(1.75, 0))
