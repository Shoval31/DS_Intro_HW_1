def convert(hours, minutes):
    if hours<0 or minutes<0:
        return("Input error!")
    else:
       h=(hours*60)-60
       H=h+hours*60*60
       M=minutes*60
       return(H+M)
       
print(convert (1.75,-9))
